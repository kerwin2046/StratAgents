# API base URL (optional). Defaults to http://localhost:8000 when not set.
# Set this when deploying the frontend to point to your API, e.g.:
# VITE_API_URL=https://ci-api-production.up.railway.app
# VITE_API_URL=http://localhost:8000
